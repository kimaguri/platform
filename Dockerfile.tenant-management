# Dockerfile for Tenant Management Service
FROM node:20-alpine

WORKDIR /app

# Install pnpm
RUN corepack enable && corepack prepare pnpm@latest --activate

# Copy package files
COPY package.json pnpm-lock.yaml ./

# Install dependencies
RUN pnpm install --frozen-lockfile

# Copy source code
COPY . .

# Install Encore CLI
RUN pnpm add -g encore.dev

# Expose service port
EXPOSE 4001

# Start the tenant management service
CMD ["encore", "run", "--port=4001"]
